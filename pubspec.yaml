name: clib_serialport_dart
description: A dart wrapper for clibserialport
version: 1.0.0

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  ffi: ^2.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
